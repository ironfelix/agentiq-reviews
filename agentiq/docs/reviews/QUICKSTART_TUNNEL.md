# 🚀 AgentIQ Reviews — Быстрый старт с localhost.run

> Запускаем MVP за 5 минут с публичным HTTPS URL

---

## Шаг 1: Первый запуск (только один раз)

```bash
cd /Users/ivanilin/Documents/ivanilin/customer-service-ai-product/agentiq/apps/reviews

# Создать виртуальное окружение
python3 -m venv venv

# Активировать
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Проверить что Redis установлен
brew install redis

# Инициализировать базу данных
python3 init_db.py
```

---

## Шаг 2: Запуск с туннелем

```bash
# Просто запусти этот скрипт:
./start-with-tunnel.sh
```

**Что происходит:**
1. ✅ Проверяется Redis
2. ✅ Запускается FastAPI (порт 8000)
3. ✅ Запускается Celery worker
4. ✅ Создаётся SSH туннель через localhost.run
5. ✅ Показывается публичный HTTPS URL

---

## Шаг 3: Настройка Telegram бота

После запуска скрипта ты увидишь что-то вроде:

```
Connect to https://abc123xyz.lhrtunnel.link
```

**Скопируй этот URL и настрой бота:**

1. Открой Telegram → @BotFather
2. Отправь: `/setdomain`
3. Выбери своего бота
4. Введи **только домен** (без https://):
   ```
   abc123xyz.lhrtunnel.link
   ```

---

## Шаг 4: Обновить .env

```bash
# Открой .env в редакторе
nano .env

# Измени FRONTEND_URL на свой публичный URL:
FRONTEND_URL=https://abc123xyz.lhrtunnel.link

# Сохрани: Ctrl+O, Enter, Ctrl+X
```

---

## Шаг 5: Перезапуск

```bash
# Останови сервисы (Ctrl+C в терминале с туннелем, или)
./stop.sh

# Запусти снова
./start-with-tunnel.sh
```

---

## Шаг 6: Тестирование

1. Открой публичный URL в браузере
2. Нажми "Login with Telegram"
3. Авторизуйся
4. Создай задачу с артикулом WB (например: 117220345)
5. Дождись уведомления в Telegram
6. Проверь отчёт

---

## 📋 Полезные команды

```bash
# Остановить все сервисы
./stop.sh

# Запустить снова с туннелем
./start-with-tunnel.sh

# Посмотреть логи FastAPI
tail -f logs/fastapi.log

# Посмотреть логи Celery
tail -f logs/celery.log

# Проверить Redis
redis-cli ping

# Проверить локальный сервер
curl http://localhost:8000/health

# Проверить публичный URL
curl https://YOUR-URL.lhrtunnel.link/health
```

---

## ⚠️ Важно знать

### URL меняется при каждом запуске
localhost.run выдаёт новый URL каждый раз. Поэтому нужно:
1. Скопировать новый URL
2. Обновить `/setdomain` в @BotFather
3. Обновить `FRONTEND_URL` в `.env`
4. Перезапустить сервисы

### Для production
localhost.run — только для тестов! Для реального запуска см. [DEPLOYMENT_CHECKLIST.md](DEPLOYMENT_CHECKLIST.md)

---

## 🐛 Troubleshooting

### SSH туннель не работает

**Попробуй альтернативы:**

```bash
# ngrok (требует регистрацию)
brew install ngrok
ngrok http 8000

# cloudflared (от Cloudflare)
brew install cloudflared
cloudflared tunnel --url http://localhost:8000

# serveo.net
ssh -R 80:localhost:8000 serveo.net
```

### Redis не запускается

```bash
# Проверить статус
brew services list

# Перезапустить
brew services restart redis

# Запустить вручную для дебага
redis-server
```

### FastAPI не отвечает

```bash
# Проверить логи
tail -f logs/fastapi.log

# Проверить что порт свободен
lsof -i :8000

# Убить процесс на порту 8000
kill $(lsof -t -i:8000)
```

### Celery не обрабатывает задачи

```bash
# Проверить логи
tail -f logs/celery.log

# Проверить что Redis работает
redis-cli ping

# Проверить очередь задач
redis-cli
> KEYS celery*
> LLEN celery
```

---

## 🎯 Следующие шаги

После успешного тестирования:

1. [ ] Провести CustDev с 2-3 продавцами WB
2. [ ] Собрать фидбек по UX/UI
3. [ ] Улучшить дизайн карточки отчёта
4. [ ] Добавить больше типов товаров в анализ
5. [ ] Deploy на production (Railway/Render)
6. [ ] Настроить домен agentiq.ru
7. [ ] Добавить onboarding
8. [ ] Добавить биллинг (если платно)

---

**Status:** ✅ Ready to use
**Last Updated:** 2026-02-05
